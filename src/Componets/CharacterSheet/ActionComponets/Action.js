import React from "react";
import styled from "styled-components";
import theme from "../../../Logic/theme";

const { primary, secondary, tri, quad, background, primaryColor } = theme;

const Action = () => {
  return <div>I am actions</div>;
};

export default Action;
